
 18-OCT-1991 : Modifications released by Herbert Greenlee
 
    - Unsolicited input (COMPACK interrupt menus) is now handled using signal
      22 (SIGPOLL).

    - Smg_delete_pasteboard now kills any subprocesses at the time it is
      called.

    - Userlib now recognizes lex source files.

    - The library release procedures now use make/userlib to compile the
      libraries.

    - There is a new utility called comp.
