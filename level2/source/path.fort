      CHARACTER*4 OLD_PATH

      CALL PATHGT(OLD_PATH)     !save original path

      CALL PATHST('FILT')

  999 CONTINUE
      CALL PATHST(OLD_PATH)     !and go back to it when you're done
      RETURN
