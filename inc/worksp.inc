C   -------COMMON,WORKSP :DUMMY COMMON TO USE ARRAYS NOT TRANSMITTED FROM
C                         ONE SUBROUTINE TO ANOTHER.MAY BE REPLACED BY
C                         'ZEBRA' WORKING SPACE.
      INTEGER LENGWS
      PARAMETER (LENGWS=5000)
      COMMON/WORKSP/WS(LENGWS)
      DIMENSION IWS(LENGWS)
      EQUIVALENCE(WS,IWS)
      REAL WS
      INTEGER IWS
