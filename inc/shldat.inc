C----------------------------------------------------------------------
C-
C-   Created  29-JAN-1990   Rajendran Raja
C-   Modified 23-JULY-1992  W. Dharmaratna, increased nhmax and nshlb
C----------------------------------------------------------------------
      INTEGER NHMAX
      PARAMETER (NHMAX=4000)
      INTEGER NSHLB
      PARAMETER( NSHLB =1760  )
      COMMON /SHLDAT /VERTEX(3),P(4,2),IDATA(7),
     &  IETA(NHMAX),IPHI(NHMAX),LAYER(NHMAX),
     &  NHIT(4),NPOINT,NHITS,NDATA,IET,IPT,
     &  TOTAL(4),CELLE(NHMAX),MG1E(NHMAX),ICDE(NHMAX),
     &  MG2E(NHMAX),CELLER(2),TOTSEC(4),EALL(4),EMISS(4),
     &  INDE(NHMAX),INDMG1(NHMAX),INDICD(NHMAX),INDMG2(NHMAX),
     &  ETOT,EIN,ETA,PHI,PX,PY,PZ,NVTX,DIST,
     &  IETAC_PRIMARY,IPHIC_PRIMARY,ETAC_PRIMARY,PHIC_PRIMARY,
     &  P4_PRIMARY(4),IPART_PRIMARY,
     &  SHLB(NSHLB)
C
      INTEGER IDATA,IETA,IPHI,LAYER,NHIT,NHITS,NDATA
      INTEGER INDE,INDMG1,INDMG2,INDICD
      REAL    TOTAL,CELLER,TOTSEC,EALL,ETOT,EIN,EMISS
      REAL    CELLE,MG1E,ICDE,MG2E
      REAL    ETA,PHI
      REAL    P,PX,PY,PZ,NVTX,DIST
      REAL    VERTEX
      INTEGER IET,IPT
      INTEGER NPOINT
      INTEGER IETAC_PRIMARY,IPHIC_PRIMARY
      INTEGER IPART_PRIMARY
      REAL    ETAC_PRIMARY,PHIC_PRIMARY
      REAL    P4_PRIMARY
      REAL    SHLB
      INTEGER ISHLB(NSHLB)
      EQUIVALENCE ( SHLB , ISHLB  )
