C----------------------------------------------------------------------
C-
C-   Created  25-MAY-1993   Jasbir Singh
C-
C----------------------------------------------------------------------
      INTEGER MAX_HITS
      PARAMETER (MAX_HITS=100)    
      INTEGER NMULIB
      PARAMETER( NMULIB =4000  )
      COMMON /MULDAT /VERTEX(3),P(4,2),IDATA(7),
     &  NHIT(4),NPOINT,NHITS,NDATA,ESTO,PSTO,
     &  ETOT,EIN,ETA,PHI,PX,PY,PZ,NVTX,DIST,TOTAL(4),
     &  IETAM_PRIMARY,IPHIM_PRIMARY,ETAM_PRIMARY,PHIM_PRIMARY,
     &  P4_PRIMARY(4),IPART_PRIMARY,PTCUR,
     &  MULIB(NMULIB)
C
      INTEGER IDATA,IETA,IPHI,LAYER,NHIT,NHITS,NDATA,PTCUR
      REAL    TOTAL,CELLER,TOTSEC,EALL,ETOT,EIN,EMISS
      REAL    ETA,PHI,ESTO,PSTO
      REAL    P,PX,PY,PZ,NVTX,DIST
      REAL    VERTEX
 
      INTEGER NPOINT
      INTEGER IETAM_PRIMARY,IPHIM_PRIMARY
      INTEGER IPART_PRIMARY
      REAL    ETAM_PRIMARY,PHIM_PRIMARY
      REAL    P4_PRIMARY
      REAL    MULIB
      INTEGER IMULIB(NMULIB)
      EQUIVALENCE ( MULIB , IMULIB  )
